FROM golang:1.24 AS build

WORKDIR /src

COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o /out/arm-sim ./cmd/arm-sim

FROM alpine:3.20

WORKDIR /app
COPY --from=build /out/arm-sim /usr/local/bin/arm-sim

CMD ["sh", "-c", "tail -f /dev/null"]
